import { style } from '@vanilla-extract/css'

export const layoutWrapper = style({
  maxWidth: '720px',
  margin: 'auto',
})

export const container = style({
  width: '100%',
  padding: '0 24px',
  margin: 'auto',
})
